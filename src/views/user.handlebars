<div>
  <div><p>First name: </p>{{user.first_name}}</div>
  <div><p>Last name: </p>{{user.last_name}}</div>
  <div><p>Email: </p>{{user.email}}</div>
  {{#if isAdmin}}
    <p>Tu role es administrador</p>
  {{/if}}
</div>